import { NgxMetricPrefixPipe } from './ngx-metric-prefix.pipe';

describe('NgxMetricPrefixPipe', () => {
  it('create an instance', () => {
    const pipe = new NgxMetricPrefixPipe();
    expect(pipe).toBeTruthy();
  });
});
